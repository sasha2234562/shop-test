{
  "name": "shop-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=./",
    "preview": "vite preview",
    "predeploy": "pnpm run build",
    "deploy": "gh-pages -d dist -f"
  },
  "devDependencies": {
    "sass": "^1.86.0",
    "vite": "^6.2.2"
  },
  "dependencies": {
    "gh-pages": "^6.3.0"
  }
}
